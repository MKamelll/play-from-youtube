### A Cli to play music from YouTube

`git clone https://github.com/MKamelll/play-from-youtube.git`

`npm install -g` in the folder.

You're gonna need `ffmpeg` installed and add the folder that has
the `ffmpeg.exe` in your path.

Run `uplay "YOUR YOUTUBE URL"`
